test_on: mac-os # run tests only on macos

tags:
  browser:
    skip: false # run tests on browser
  chrome:
    skip: "chrome is not supported on macos" # skip tests on chrome on macos
  firefox:
    skip: windows # skip tests on firefox on windows